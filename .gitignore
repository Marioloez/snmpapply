# Secrets and real device lists (contain client IPs/credentials).
# The *.example files ARE tracked as templates.
.env
inventory.json

# Community backups (contain the client's existing SNMP community values)
community-backup-*.json

# Build output
/dist/
/snmpapply
